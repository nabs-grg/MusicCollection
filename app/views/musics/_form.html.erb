<%= simple_form_for @music do |f| %>
	<%= f.input :album, lablel: "Album Title"%>
	<%= f.input :track%>
	<%= f.input :artist%>
	<%= f.input :genre%>
	<%= f.input :released_year%>
	<%= f.button :submit%>
<% end %>